{{!
    @template block_mission_map/map
}}
<div class="map">
    {{#chapters}}
        <div class="chapter">
            <h3>{{name}}</h3>
            {{#levels}}
                <a href="http://localhost/moodle/blocks/mission_map/level.php?chapterid={{cid}}&levelid={{lid}}" class="mission">
                    <span class="mission-text">
                        {{id}}
                    </span>
                </a>
            {{/levels}}
        </div>
    {{/chapters}}
</div>