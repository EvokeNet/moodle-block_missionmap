{{!
    @template block_mission_map/blank
}}
<div>
        <button
            class="btn btn-secondary m-l-0"
            name="Configure chapters"
        >
        {{{url}}}
        </button>
</div>